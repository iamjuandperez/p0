prueba = '(defvar rotate 3) (defun blocked-p () (not (can-move-p :north))) ((if (blocked-p) (move 1) (skip)) (turn :left)) (defvar one 1) (defun foo (c p) (put :chip c) (put :ballon p) (move rotate)) (foo 1 3) (defun goend () (if (not blocked-p) ((move one) (goend)) (skip))) (defun pickAllB () (pick :balloons ballonsHere)) (run-dirs :left :up :left :down :right)'